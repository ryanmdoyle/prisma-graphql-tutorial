import { ISDL, IGQLType } from 'prisma-datamodel';
import { INormalizer } from './normalizer';
export declare class HideReservedFields implements INormalizer {
    protected baseModel: ISDL;
    static reservedFields: string[];
    constructor(baseModel: ISDL);
    normalizeType(type: IGQLType, ref: IGQLType): void;
    normalize(model: ISDL): void;
}
